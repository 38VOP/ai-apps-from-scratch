# QA_BACKLOG — 3d-hub-aggregator

Інструмент фіксації знахідок. Веде координатор.
Guardian бере звідси зріз для планування подальшої роботи.

**Легенда severity:** Critical (втрата даних / непрацездатність) · High (функція зламана) · Medium (працює хибно, є обхід) · Low (косметика)
**Складність:** S — точкова правка одного файлу · M — кілька файлів / логіка · L — потребує обговорення
**Antigravity-зона:** чи перетинається файл із зоною дизайн-треку Antigravity (`Catalog.jsx`, `index.css`, розмітка/CSS сайдбару й модалок)

**Правила ведення:**
- Це **інструмент фіксації, не автономна ініціатива**. Напрямок і пріоритети перевірок визначає власник — що саме тестувати і коли
- Координатор **не запускає повні QA-проходи без вказівки** і **не присвоює важливість самостійно**: severity фіксується як спостережений факт, остаточна пріоритизація за Guardian
- Кожен запис має **файл(и)** і **складність** — щоб Guardian ставив задачу без попередньої розвідки
- У «Закрито» фіксується **причина** (не лише симптом) і **як саме перевірено** — щоб помилка не повторилась і щоб було видно, що підтвердження фактичне, а не самозвіт
- **Закриття записів — тільки Guardian**, після підтвердження кодом. Координатор переносить запис у «Закрито» лише за його вказівкою
- Розділ «Перевірено, дефектів не знайдено» існує, щоб Guardian не витрачав ітерації на вже перевірене
- Записи зі статусом «Чекає рішення» координатор не виправляє самостійно
- **Пов'язані записи** позначаються як «виконувати ПІСЛЯ QA-XXX» з поясненням причини порядку — не «заблоковано». Запис не заблокований по суті, обмежена лише послідовність, і Guardian має бачити, чому саме
- Регреси, внесені координатором, фіксуються з прямою вказівкою на це (див. QA-006) — картина має бути повною, не лише успіхи

---

## Відкриті

| ID | Що не так | Де | Severity | Файл(и) | Складність | Antigravity-зона | Статус |
|---|---|---|---|---|---|---|---|
| QA-001 | Швидкий подвійний клік на «Додати в кошик» створює ДУБЛЬ моделі в кошику. Race condition: два паралельних POST проходять перевірку `existing` до першого коміту. Послідовні запити дедуплікуються правильно («Модель вже у кошику»), паралельні — ні. Немає UNIQUE-констрейнта на `cart_items.model_id` | Каталог → кнопка кошика на картці | **High** | `backend/routers/cart.py`, `backend/database.py` | M | Ні (backend) | **Виправлено `a5efae5`, підтверджено Guardian кодом.** Фікс: (1) `unique=True` на `CartItem.model_id`; (2) міграція в `migrate_sqlite_columns()` — дедуплікація наявних рядків + `CREATE UNIQUE INDEX ux_cart_items_model_id`; (3) `IntegrityError` у POST → rollback і повернення наявного запису замість 500. Перевірка: 10 паралельних POST → `count: 1`, 0 дублів, рівно один «додано», решта «вже у кошику»; 6 швидких кліків в UI → бейдж `1` (раніше 2 кліки давали `3`). Індекс підтверджено в БД, дані цілі (6483 моделі) |
| QA-002 | ~~Бейдж кошика не оновлюється після видалення позиції~~ — **ХИБНИЙ ЗАПИС, дефекту немає**. Перевірено в браузері з перехопленням `fetch`: видалення позиції → `DELETE /api/cart/5` → `GET /api/cart` → бейдж `2→1`, модалка оновилась. «Очистити кошик» → `DELETE /api/cart` → `GET /api/cart` → бейдж `0`. Код `handleRemoveFromCart` і `fetchCart` коректні. **Причина хибного запису — моя методика:** тоді я видаляв позицію через `curl` у терміналі, а не через UI, тож фронтенд не мав звідки дізнатися про зміну в БД. Бейдж показував актуальний на той момент стан, не застарілий. **Урок: перевіряти UI-поведінку тільки через UI**, зміни через API поза застосунком не є валідним тестом фронтенду | Шапка, після видалення з кошика | ~~Medium~~ → **не дефект** | — | — | Ні | **Знято з обліку, підтверджено Guardian** |
| QA-003 | `.git` роздутий до 469 МБ через історично закомічені `venv/`, `dist/`, `catalog.db`. Кожен push перепаковує репозиторій | інфраструктура | Medium | історія git | L | Ні | **Не чіпати** — розмір не впливає на роботу, відкладено свідомо |
| QA-004 | ~~Блок «Налаштування» — окремий розділ~~ навігація вже реалізована (коміт `8665489`, 22.08.2026, разом із QA-008 — випало з трекінгу при переносі Guardian). **Лишилось тільки:** перенести індикатор версії з `SourcesPanel.jsx` у розділ «Налаштування» | Розділ «Налаштування» | Low | `SourcesPanel.jsx` → куди саме в «Налаштування» | S | Ні | Звужено до одного пункту, підтверджено кодом 02.09.2026. Питання власника: у «Налаштуванні» зараз немає власного екрану (тільки підменю Категорії/Джерела/Статистика) — куди саме має піти індикатор, потрібне рішення |
| QA-005 | Пагінація каталогу | Каталог | Low | `Catalog.jsx` | L | **Так** | **Виконано `0012d50`** — нумеровані сторінки (вікно 5 номерів), ««/»» на першу-останню, перемикач 12/24/48/96 на сторінку. Чекає підтвердження Guardian |
| QA-008 | Кнопка «Налаштувати» стояла між списком категорій і секцією «Проекти», порушуючи ANCHOR.md р.86. Недоробка мого `9c21f1e` | Сайдбар каталогу | Low | `Catalog.jsx` | S | **Так** | **Закрито варіантом C (`8665489`)** — кнопку прибрано з блоку категорій, її функція перенесена у слот «Категорії» блоку «Налаштування». Порядок тепер: КАТЕГОРІЇ(10) → ПРОЕКТИ(3) → НАЛАШТУВАННЯ(3), тобто «Проекти» одразу під списком категорій, як вимагає якір. Чекає підтвердження Guardian |

### Порція від власника (тестовий прохід, 22.08.2026)

| ID | Що не так | Де | Severity | Файл(и) | Складність | Antigravity-зона | Статус |
|---|---|---|---|---|---|---|---|
| QA-009 | Немає кнопки «Назад» зі стрілкою на підсторінках. Власник: «якщо з головної через кнопки переходимо в інше поле, то повернення має бути кнопкою бек зі стрілкою на кожній сторінці, щоб повернутися тим самим шляхом, яким попали». Застосувати **однаково до всіх** розділів (Джерела, Статистика, Проекти), не по-різному на кожній сторінці | Усі підсторінки | Medium | `SourcesPanel.jsx`, `AdminDashboard.jsx`, `Projects.jsx` | M | **Так** (розмітка секцій) | **Виконано `8cfa58b`** — .back-btn на трьох підсторінках, align-self:flex-start. Підтверджено Guardian кодом 22.08.2026 (запис був лише в GUARDIAN_LOG.md, статус тут не оновили — виправлено 25.08.2026) |
| QA-010 | Вкладка «Каталог» у шапці підлягає видаленню. Власник вказав, що вона мала зникнути ще в попередніх задачах | Шапка | Medium | `App.jsx` | S | Ні | **Виконано, підтверджено кодом** — шапка містить лише лого/пошук/кошик, навігація повністю в сайдбарі |
| QA-011 | «Проекти» в сайдбарі мають дзеркалити патерн «Категорій»: додати пункт **«Усі проекти N»** першим у списку, далі перелік проектів. Клік на «Усі проекти» → сторінка з усіма; клік на конкретний → його вміст. **Цифра = кількість проектів**, не сумарна кількість моделей | Сайдбар каталогу | Medium | `Catalog.jsx` | M | **Так** | **Виконано, підтверджено кодом** — «Усі проекти» з badge-count, далі перелік з `item_count` на кожному, точний дзеркальний патерн |
| QA-012 | Колонки на сторінці «Джерела» рівної ширини (652/652), через що права колонка **обрізається за межею екрану** — останній стовпець таблиці `МОНІТОРИНГ` не видно, власник не може дописати зауваження. Ліва колонка (назва/телефон/кількість каналів) не потребує стільки пікселів і має нижчий пріоритет; права з таблицею на 7 стовпців — основна. Потрібна пропорція ~1:2. **Мій регрес `fe01de9`** — зробив `flex: 1 1 0` для обох замість пріоритетних | Сторінка «Джерела» | **High** | `index.css` (`.sources-two-col`) | S | **Так** | **Виконано `991f74e`** — min-width:0 + .table-scroll, перевірено на 5 ширинах. Підтверджено Guardian кодом 22.08.2026 (статус тут не оновили — виправлено 25.08.2026) |
| QA-013 | Статус каналу «Помилка» — червона точка без причини. Потрібен **людський опис + дія**, не технічний код | Таблиця каналів | **High** | `backend/routers/sources.py`, `SourcesPanel.jsx` | M | Частково | **Механізм виконано, підтверджено кодом** — `status_message` заповнюється людською фразою, tooltip підключено. **Не перевірено окремо:** супутня знахідка про конкретні розбіжності («Помилка» при 703 моделях і успішному парсингу, «У черзі» з оновленням «Ніколи») — чи видно тепер причину саме в цих випадках, чи це були одноразові дані, що вже застаріли |
| QA-014 | Керування акаунтами: (1) **заборонити дефолтні назви** — назва обов'язкова і має відповідати акаунту, бо зараз той самий Telegram доданий двічі як «VOPke» і «Основний акаунт» без різниці; (2) **нумерація акаунтів** для контролю кількості; (3) **ліміт ~10 акаунтів** — важливо для організації безпеки й для того, щоб чітко ідентифікувати, з якого акаунта прийшли дані у майбутній статистиці; (4) технічну заготовку, що з'являється на порожній платформі, **позначати як тестову** (у готовому продукті вона не потрібна взагалі — користувач просто додасть свій Telegram) | Розділ «Джерела», акаунти | Medium | `backend/routers/sources.py`, `SourcesPanel.jsx`, `backend/database.py` | M | Частково | Відкрито |
|| QA-015 | У БД три акаунти, з яких реально працює один: «Основний Telegram Акаунт» (телефон не вказано, не авторизований, 0 каналів — технічна заготовка) і «Основний акаунт» (той самий номер, що «VOPke», 0 каналів — дубль). Канали прив'язані лише до «VOPke». Це **дані, не код** | БД, таблиця акаунтів | Low | дані | S | Ні | **Дозволено точковий виняток із integrity FREEZE (25.08.2026, рішення Guardian) — передано координатору з обов'язковою SELECT-перевіркою перед DELETE. Виконано: видалено id=4 (VOPke2, дубль) і id=16 (пусте ім'я). Було 4, стало 2. VOPke5 (id=15) лишився недоторканний. Чекає підтвердження Guardian |
| QA-019 | Структурний розрив is_authorized: ліва колонка читає поле БД і показує "З'єднано", але при синхронізації queue_channel перевіряє реальну сесію Telethon і падає з "не авторизований", бо сесія мертва. Поле is_authorized ніколи не скидається назад. Дві частини UI суперечили одна однілі | Синхронізація, список акаунтів | **High** | `backend/telegram_service.py` (queue_channel, sync_channel_posts, get_client_for_account), `frontend/src/App.jsx` (handleSyncChannel) | M | Ні | **Виконано 40475a6** — write-through is_authorized при виявленні мертвої сесії + fetchAccounts у фронтенді. Чекає підтвердження Guardian |
|| QA-018 | Канали обробляються в порядку додавання (FIFO), неможливо змінити пріоритет. **Додаткові вимоги:** (1) Потрібна інформативність — показувати скільки відскановано із скільки (прогрес); (2) Потрібна гнучкість — при потребі власник повинен мати можливість змінити черговість сканування каналів | Таблиця каналів, блок "Telegram-канали для моніторингу" | Medium | `backend/telegram_service.py`, `backend/database.py`, `SourcesPanel.jsx` | M | Ні | **Очікує рішення Guardian** — запит на погодження змін (пріоритет каналів, прогрес-індикатори, зміна черговості) направлено |
| QA-016 | «Переавторизувати» на картці акаунта — глухий кут. `handleCardReauth` шле `POST /request-code` і показує «Код надіслано! Перевірте Telegram», але на картці НЕМАЄ поля для вводу коду і виклик модалки не відкривається — користувачу фізично нема куди ввести код. Підтверджено власником: другий клік «Переавторизувати» (бо перший ніби нічого не зробив) → Telegram: «вичерпав способи доставки коду». Це не помилка користувача — Telegram справедливо реагує на два `send_code_request` поспіль без завершення першого | Картка акаунта, розділ «Джерела» | **High** | `SourcesPanel.jsx` (`handleCardReauth`) | M | Ні | Відкрито — знайдено Guardian 25.08.2026 при аналізі скріншота власника |
| QA-017 | Сторінка «Джерела» не використовує всю ширину екрану — праворуч велика порожня смуга. **Загальне правило вже внесено в ANCHOR.md** (секція «Дизайн») — не ставити px `max-width` на головний контейнер сторінки, крім вузьких форм/модалок | Сторінка «Джерела» | Low | `index.css` + QA-020 | S | Так | **Виконано, симптом підтверджено зник** — DevTools показав `maxWidth: none`, повна ширина на Sources/Projects/Admin після `c1e84f6` (QA-020). Дві проміжні хибні тривоги (Dark Reader extension, застарілий кеш dev-сервера) — не код |
| QA-020 | Та сама «порожня смуга праворуч», але виявлено ширше за QA-017: `maxWidth: 1400/1100` стоїть інлайн-стилем прямо на `<main className="content-area">` у трьох компонентах — `SourcesPanel.jsx` (1400), `Projects.jsx` (1100), `AdminDashboard.jsx` (1100). Порушує те саме правило ANCHOR.md («Дизайн»), тільки в JSX, не CSS, і зачіпає 3 сторінки, не одну | Sources/Projects/AdminDashboard | Low | 3 файли `.jsx` | S | Так | **Виконано `c1e84f6`, підтверджено кодом** — inline maxWidth прибрано з усіх трьох. Побічний коміт `ae1fcb7` прибрав 4 сторонніх debug-файли, що випадково потрапили в c1e84f6 |

---

## Перевірено, дефектів не знайдено (обхід 22.08.2026, ніч)

Метод: перехоплення `console.error`/`console.warn`, `window.onerror`, `unhandledrejection` і не-2xx `fetch` через ін'єкцію хуків після завантаження сторінки.

| Перевірка | Результат |
|---|---|
| JS-консоль на всіх вкладках (Каталог / Джерела / Статистика / Проекти) | **0 помилок, 0 попереджень** |
| Не-2xx fetch-запити під час навігації і пошуку | **0** |
| Uncaught exceptions / unhandled promise rejections | **0** |
| Зображення карток (24 шт) | 0 битих (`naturalWidth === 0`) |
| Пошук: спецсимволи `'"<>&%_` | без помилок |
| Пошук: SQL-подібне `' OR 1=1 --` | без помилок, ін'єкції немає |
| Пошук: 300 символів | без помилок |
| Пошук: юнікод/емодзі/арабська `🐴模型لوستر` | без помилок |
| Пошук: скидання поля → повернення 24 карток | коректно |
| Empty state при `zzzqqq` | коректний текст-підказка |
| Модалка картки моделі: відкриття / закриття по overlay | коректно |
| Модалка кошика: відкриття | коректно |
| Фільтрація end-to-end | API `6483 → 55` при `search=DECO` |

---

## Закрито

| ID | Що було | Причина | Перевірка | Коміт |
|---|---|---|---|---|
| QA-007 | Клік по будь-якому проекту в сайдбарі відкривав однаковий загальний список «Мої проекти» замість вмісту саме цього проекту — зайвий крок і візуальний шум | Сайдбар викликав лише `setActiveTab('projects')`, не передаючи, який проект відкрити. `Projects.jsx` показував список-посередник завжди, а деталі — під ним | Проект 3 → `آبجکت_میز` · проект 2 → `آبجکت_مبل`, `آبجکت_کمد` · проект 1 → `آبجکت_موبایل`. Кожен свої моделі, список схований, кнопка «Усі проекти» повертає всі 3 картки | `5fea5f5` |
| QA-006 | Клік на заголовок «Проекти» в сайдбарі провалював у розділ замість згортання | Регрес мого ж `9c21f1e`: на одному `onClick` висіли і `setProjectsExpanded`, і `setActiveTab`. У «Категоріях» другого виклику немає | Іконка `−`→`+`→`−`, список 3→0→3, `still_on_catalog: true` на кожному кроці. Клік по конкретному проекту не зламано | `d227ebd` |
| — | Немає поля пошуку в шапці | Поле не існувало в JSX узагалі, хоча CSS `.search-box`/`.search-input` були готові в `index.css` (р.156-187) | `search_left_of_cart: true` (x=1180 проти x=1516). Фільтрація end-to-end: API `6483 → 55` при `search=DECO` | `e32a6ce` |
| — | Слабкі контури блоків по всій сторінці | `--border-color: rgba(255,255,255,0.08)` — 8% білого на темному фоні | `getComputedStyle`: `.model-card`, `.sidebar`, `.app-header` — усі `rgba(255,255,255,0.16)`. Одна змінна → 41 елемент | `10731c8` |
| — | Індикатор версії: два `<span>` без текстового роздільника, читабельність трималась на CSS-відступах | Розділення лише flex-layout, при копіюванні й програмному зчитуванні текст злипався | `span_count: 1`, `dot_separators: 2` (у самому тексті), формат `DD.MM.YYYY, HH:MM` | `beb6825` |
| — | Індикатор версії статичний («v1.0.0», «Build: 2026-08-20») — вимагав ручного редагування | Хардкод у JSX замість build-time підстановки | Два послідовні заміри: HEAD `9c21f1e` → показав `9c21f1e`; після merge HEAD `3105440` → показав `3105440`. Хеш і дата змінюються самі | `3ae1785` |
| — | `catalog.db` відслідковувався git, блокував `rebase` при живому backend (`unable to unlink: Invalid argument`) | `.gitignore` лежав у `backend/`, правило `data/` не покривало `data/` рівнем вище. Живий процес тримав SQLite відкритим | Розмір до/після `git rm --cached` ідентичний (3 809 280 б), дані цілі (6483 моделі). Наступний `pull --rebase` пройшов fast-forward без блокувань | `3ae1785` |
| — | Розділ «Проекти» недосяжний з UI | Компонент `Projects.jsx` (176 рядків, повний CRUD) був захищений умовою `activeTab !== 'projects'`, але `setActiveTab('projects')` не викликався НІДЕ в кодовій базі. У шапці три вкладки без «Проектів». Осиротілий стан `projectsExpanded` — слід незавершеної реалізації | Сайдбар показав 3 проекти з лічильниками, клік відкрив «Мої проекти» з датами | `9c21f1e` |
| — | Бейдж кошика зникав при 0 — стан кошика доводилось вгадувати | Умова `cartCount > 0 &&` у JSX | У бандлі зникла умова `ut>0&&`; у браузері бейдж показує `0` при порожньому кошику | `8997697` |
| — | Кнопка «Додати в кошик» не працювала | **Не код.** Осиротілий backend-процес від 17.08 слухав `127.0.0.1:8000` (специфічніший за `0.0.0.0:8000` власника, тому Windows віддавав запити йому) і працював на версії без `routers/cart.py`. `/openapi.json` віддавав 14 маршрутів замість 22, `/api/cart` відсутній → 404 → `data.success` undefined → `fetchCart()` не викликався → кнопка «мовчала» | Після перезапуску: 22 маршрути, `/api/cart` у списку. Власник підтвердив, що додавання в кошик і створення проектів працюють | без правок коду, перезапуск backend |
| — | `git push` зависав 10+ хвилин (зафіксовано 567 с) при справній мережі | `GIT_TRACE=1` показав `run_command: 'git credential-manager get'` — глобальний `credential.helper=manager` відкривав GUI-вікно авторизації, яке нікому було закрити | Після пінування `credential.helper=store` + `credential.interactive=false` у локальному конфізі репо push пройшов за секунди | локальний конфіг репо |
