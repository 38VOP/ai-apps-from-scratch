# QA_BACKLOG — 3d-hub-aggregator

Єдине джерело правди для знайдених дефектів і побажань. Веде координатор.
Guardian бере звідси зріз для планування наступного апдейта.

**Легенда severity:** Critical (втрата даних / непрацездатність) · High (функція зламана) · Medium (працює хибно, є обхід) · Low (косметика)
**Складність:** S — точкова правка одного файлу · M — кілька файлів / логіка · L — потребує обговорення

---

## Відкриті

| ID | Що не так | Де | Severity | Файл(и) | Складність | Статус |
|---|---|---|---|---|---|---|
| QA-001 | Швидкий подвійний клік на «Додати в кошик» створює ДУБЛЬ моделі в кошику. Race condition: два паралельних POST проходять перевірку `existing` до першого коміту. Послідовні запити дедуплікуються правильно («Модель вже у кошику»), паралельні — ні. Немає UNIQUE-констрейнта на `cart_items.model_id` | Каталог → кнопка кошика на картці | **High** | `backend/routers/cart.py`, `backend/database.py` (UNIQUE), опційно debounce в `App.jsx` | M | Відкрито |
| QA-002 | Бейдж кошика не оновлюється після видалення позиції — показував `3`, тоді як статистика і БД давали `2`. Виправляється лише F5. Ознака: `fetchCart()` не викликається після успішного DELETE, або результат ігнорується | Шапка, після видалення з кошика | **Medium** | `frontend/src/App.jsx` (`handleRemoveFromCart`) | S | Відкрито |
| QA-003 | `.git` роздутий до 469 МБ через історично закомічені `venv/`, `dist/`, `catalog.db`. Кожен push перепаковує репозиторій. Очищення історії — деструктивна операція | інфраструктура | Medium | історія git | L | Чекає рішення Guardian |
| QA-004 | Блок «Налаштування» — окремий розділ (винести туди індикатор версії з «Джерел») | навігація | Low | нове | L | Відкладено до живого діалогу |
| QA-005 | Пагінація каталогу | Каталог | Low | `Catalog.jsx` | L | Відкладено до живого діалогу |

---

## Перевірено, дефектів не знайдено (обхід 22.08.2026, ніч)

Метод: перехоплення `console.error`/`console.warn`, `window.onerror`, `unhandledrejection` і не-2xx `fetch` через ін'єкцію хуків після завантаження сторінки.

| Перевірка | Результат |
|---|---|
| JS-консоль на всіх вкладках (Каталог / Джерела / Статистика / Проекти) | **0 помилок, 0 попереджень** |
| Не-2xx fetch-запити під час навігації і пошуку | **0** |
| Uncaught exceptions / unhandled promise rejections | **0** |
| Зображення карток (24 шт) | 0 битих (`naturalWidth === 0`) |
| Пошук: спецсимволи `'"<>&%_` | без помилок |
| Пошук: SQL-подібне `' OR 1=1 --` | без помилок, ін'єкції немає |
| Пошук: 300 символів | без помилок |
| Пошук: юнікод/емодзі/арабська `🐴模型لوستر` | без помилок |
| Пошук: скидання поля → повернення 24 карток | коректно |
| Empty state при `zzzqqq` | коректний текст-підказка |
| Модалка картки моделі: відкриття / закриття по overlay | коректно |
| Модалка кошика: відкриття | коректно |
| Фільтрація end-to-end | API `6483 → 55` при `search=DECO` |

---

## Закрито

| ID | Що було | Коміт |
|---|---|---|
| — | Кнопка «Додати в кошик» не працювала — причина інфраструктурна (процес-привид від 17.08 на `127.0.0.1:8000` без роутера `/api/cart`) | без правок коду, перезапуск backend |
| — | Бейдж кошика зникав при 0 | `8997697` |
| — | Розділ «Проекти» недосяжний з UI | `9c21f1e` |
| — | Індикатор версії статичний | `3ae1785` |
| — | Індикатор версії: два span без текстового роздільника | `beb6825` |
| — | Слабкі контури блоків (`--border-color` 8%) | `10731c8` |
| — | Немає поля пошуку в шапці | `e32a6ce` |
| QA-006 | Клік на заголовок «Проекти» в сайдбарі провалював у розділ замість згортання (регрес мого ж `9c21f1e` — два onClick-виклики на одному хендлері) | `d227ebd` |
| — | `catalog.db` відслідковувався git, блокував rebase | `3ae1785` |
| — | `git push` зависав 10+ хв (GCM GUI-промпт) | локальний конфіг репо |
